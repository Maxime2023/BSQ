/*
** EPITECH PROJECT, 2018
** one
** File description:
** one
*/

#include "../include/header.h"

int    one(char *buf)
{
    int a = 0;

    if (buf[a + start(buf)] == 'o')
        my_putchar('o');
    if (buf[a + start(buf)] == '.')
        my_putchar('x');
    my_putchar('\n');
    exit (0);
}
